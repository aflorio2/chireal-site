source "https://rubygems.org"

# jekyll
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.7"

# Use jekyll-sass-converter 2.x instead of 3.x to avoid sass-embedded
gem "jekyll-sass-converter", "~> 2.2"

# Ruby 3.3+ compatibility
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"

# Remove html-proofer for now (it depends on posix-spawn which has Ruby 3.3 issues)
# gem "html-proofer", "~> 5.0"

# plugins
group :jekyll_plugins do
  gem "jekyll-spaceship"
  gem "jekyll-sitemap"
  gem "jekyll-redirect-from"
  gem "jekyll-feed"
end
